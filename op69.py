# define a class "Media" with attributes "title" & "price" & method to accept & display data
# define a class "Book" which inherits "Media" & additional attribute "no_pages"
# define a class "CD" which inherits "Media" & additional attribute "size_mb"
# accept data of "n" items (book or cd) & display details
